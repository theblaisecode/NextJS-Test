export type NavMenu = {
  id: number;
  url: string;
  ariaLabel: string;
  title: string;
};

export type Tour = {
  id: string;
  name: string;
  info: string;
  image: string;
  price: string;
};

export type User = {
  id: string;
  firstName: string;
  lastName: string;
};
